defaults: &defaults
  adapter: postgresql
  encoding: unicode
  database: onebox_core_test
  pool: 5
  username: postgres
  password:
  template: template0
  host: localhost

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults